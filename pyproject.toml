[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bosai-studio-control-plane"
version = "0.4.0"
description = "Governed autonomous operations for AI-powered media production."
requires-python = ">=3.11"
dependencies = [
  "google-adk[mcp]==2.5.0",
  "google-auth[aiohttp]>=2.56,<3",
  "pydantic>=2.11,<3",
  "opentelemetry-sdk>=1.42,<2",
  "opentelemetry-exporter-otlp-proto-http>=1.42,<2",
]

[tool.setuptools.packages.find]
where = ["src"]
